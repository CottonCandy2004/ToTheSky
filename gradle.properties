# 组织
org.gradle.jvmargs=-Xmx3G -Dcom.sun.net.ssl.checkRevocation=false
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

# Clash 增强模式/TUN 已接管出网，不再设置 JVM 代理，避免 MCPRepo 被二次 CONNECT reset。
# 如需显式代理，可取消下面注释：
# systemProp.http.proxyHost=127.0.0.1
# systemProp.http.proxyPort=7890
# systemProp.https.proxyHost=127.0.0.1
# systemProp.https.proxyPort=7890

forge_version=47.4.10
minecraft_version=1.20.1
minecraft_version_range=[1.20.1]
forge_version_range=[47,)
loader_version_range=[47,)

# 模组身份
mod_id=tothesky
mod_name=ToTheSky
mod_license=All Rights Reserved
mod_version=1.0.0
mod_group_id=com.fst.tothesky
mod_authors=RiaAED
# Create 系依赖版本（开发环境用 slim + Maven，见 build.gradle）
create_version=6.0.8-289
ponder_version=1.0.91
flywheel_version=1.0.5
registrate_version=MC1.20-1.3.3
